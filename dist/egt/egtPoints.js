const EgtPoints=(function(){const STANDINGS_EXCLUDED_ROUNDS=["R1"];const ROUND_MAX_POINTS={R1:6,R2:4,R3:4,R4:5,R5:4,R6:7};function rankedFinishPoints(ranked,slots){const out={};ranked.forEach(r=>{out[r.player]=0});let i=0;while(i<ranked.length){let j=i;while(j+1<ranked.length&&ranked[j+1].value===ranked[i].value)j++;const size=j-i+1;const pool=slots.slice(i,i+size).reduce((a,b)=>a+(b||0),0);const share=pool/size;for(let k=i;k<=j;k++)out[ranked[k].player]=share;i=j+1}return out}function addPoints(acc,pid,category,pts){if(!acc[pid])acc[pid]={total:0,breakdown:[]};acc[pid].total+=pts;acc[pid].breakdown.push({category,points:pts})}function pointsForRound(model,roundId,results,acc){const cfg=model.pointsConfig[roundId]||{};const round=model.rounds.find(r=>r.id===roundId);const teamPlayers=name=>{const t=(round.teams||[]).find(x=>x.name===name);return t?t.players:[]};if(roundId==="R1"){if(results.bbb)results.bbb.champions.forEach(pid=>{addPoints(acc,pid,"BBB champion",(cfg.bbbChampion||0)/results.bbb.champions.length)});if(results.nines)results.nines.champions.forEach(pid=>{addPoints(acc,pid,"Nines champion",(cfg.ninesChampion||0)/results.nines.champions.length)})}else if(roundId==="R2"&&results.fourBall){const seg=results.fourBall.segments;const award=(m,label,pts)=>{if(m.winnerTeam==="halve")return;teamPlayers(m.winnerTeam).forEach(pid=>addPoints(acc,pid,label,pts))};award(seg.front,"Nassau front",cfg.nassauFront||1);award(seg.back,"Nassau back",cfg.nassauBack||1);award(seg.overall,"Nassau overall",cfg.nassauOverall||2)}else if(roundId==="R3"&&results.wolf){const ranked=results.wolf.ranked.map(r=>({player:r.player,value:r.units}));const pts=rankedFinishPoints(ranked,cfg.wolfFinish||[4,3,2,1]);Object.entries(pts).forEach(([pid,p])=>addPoints(acc,pid,"Wolf finish",p))}else if(roundId==="R4"&&results.teamStableford){const ts=results.teamStableford;ts.segments.forEach((s,i)=>{if(s.winner==="halve")return;teamPlayers(s.winner).forEach(pid=>addPoints(acc,pid,`Stableford seg ${i+1}`,cfg.segmentWin||1))});if(ts.overallWinner!=="halve")teamPlayers(ts.overallWinner).forEach(pid=>addPoints(acc,pid,"Stableford 18 total",cfg.overall18||2))}else if(roundId==="R5"){if(results.bbb)results.bbb.champions.forEach(pid=>{addPoints(acc,pid,"BBB champion",(cfg.bbbChampion||2)/results.bbb.champions.length)});if(results.matchPlay)results.matchPlay.champions.forEach(pid=>{addPoints(acc,pid,"Match play champion",(cfg.matchPlayChampion||2)/results.matchPlay.champions.length)})}else if(roundId==="R6"){if(results.singles)results.singles.results.forEach(m=>{if(m.winner==="halve"){addPoints(acc,m.a,"Singles halve",cfg.singlesHalve||1.5);addPoints(acc,m.b,"Singles halve",cfg.singlesHalve||1.5)}else{addPoints(acc,m.winner,"Singles win",cfg.singlesWin||3)}});if(results.stableford){const ranked=results.stableford.ranked.map(r=>({player:r.player,value:r.points}));const pts=rankedFinishPoints(ranked,cfg.stablefordFinish||[4,3,2,1]);Object.entries(pts).forEach(([pid,p])=>addPoints(acc,pid,"Stableford finish",p))}}return acc}function seasonAwards(model,seasonStats,skinsTotals,acc){const cfg=model.pointsConfig.seasonAwards||{};const winnersOf=(valueOf,lowest)=>{const entries=model.players.map(p=>({pid:p.id,v:valueOf(p.id)}));const best=lowest?Math.min(...entries.map(e=>e.v)):Math.max(...entries.map(e=>e.v));return entries.filter(e=>e.v===best&&isFinite(e.v)).map(e=>e.pid)};const grant=(winners,label,pts)=>winners.forEach(pid=>addPoints(acc,pid,label,pts/winners.length));grant(winnersOf(pid=>(skinsTotals==null?void 0:skinsTotals[pid])||0,false),"Skins King",cfg.skinsKing||2);grant(winnersOf(pid=>{var _a;return((_a=seasonStats==null?void 0:seasonStats[pid])==null?void 0:_a.netBirdies)||0},false),"Birdie King (net)",cfg.birdieKingNet||2);grant(winnersOf(pid=>{const s=seasonStats==null?void 0:seasonStats[pid];return s&&s.puttHoles>0?s.putts:Infinity},true),"Flat Stick (fewest putts)",cfg.flatStickFewestPutts||1);grant(winnersOf(pid=>{var _a,_b;return(((_a=seasonStats==null?void 0:seasonStats[pid])==null?void 0:_a.fairwaysHit)||0)+(((_b=seasonStats==null?void 0:seasonStats[pid])==null?void 0:_b.greensInReg)||0)},false),"Iron Man (FIR+GIR)",cfg.ironManFIRplusGIR||1);return acc}function compute(model,resultsByRound,seasonInputs){const acc={};model.players.forEach(p=>{acc[p.id]={total:0,breakdown:[]}});model.rounds.forEach(round=>{if(STANDINGS_EXCLUDED_ROUNDS.includes(round.id))return;if(resultsByRound[round.id])pointsForRound(model,round.id,resultsByRound[round.id],acc)});if(seasonInputs&&seasonInputs.final){seasonAwards(model,seasonInputs.seasonStats,seasonInputs.skinsTotals,acc)}return acc}function roundPointsBreakdown(model,roundId){const cfg=(model.pointsConfig||{})[roundId]||{};const nth=i=>["1st","2nd","3rd","4th"][i]||`${i+1}th`;switch(roundId){case"R1":return{roundId,mode:"none",max:0,items:[],summary:"Cash only",note:"Flat / stakes-only round — BBB, The Nines and skins all pay cash, but no EGT Cup points are awarded."};case"R2":{const f=cfg.nassauFront||1,b=cfg.nassauBack||1,o=cfg.nassauOverall||2;return{roundId,mode:"team",max:f+b+o,items:[{label:"Win the front-9 match",pts:f},{label:"Win the back-9 match",pts:b},{label:"Win the 18-hole match",pts:o}],summary:`Front ${f} · Back ${b} · Overall ${o}`,note:"Four-ball is a 2v2 team event: both players on the winning team earn the full value of each match won. A halved match awards no points."}}case"R3":{const slots=cfg.wolfFinish||[4,3,2,1];return{roundId,mode:"individual",max:slots[0]||0,items:slots.map((pts,i)=>({label:`Finish ${nth(i)} in Wolf units`,pts})),summary:`Wolf-units finish ${slots.join(" / ")}`,note:"Everyone scores: finish order by Wolf units pays every spot. Tied places pool and split their points."}}case"R4":{const seg=cfg.segmentWin||1,o18=cfg.overall18||2;return{roundId,mode:"team",max:seg*3+o18,items:[{label:"Win holes 1–6",pts:seg},{label:"Win holes 7–12",pts:seg},{label:"Win holes 13–18",pts:seg},{label:"Win the 18-hole total",pts:o18}],summary:`Segments ${seg}+${seg}+${seg} · 18-hole total ${o18}`,note:"Aggregate Stableford is a 2v2 team event: both players on the winning team earn the full value of each segment won. Halved segments award no points."}}case"R5":{const bbb=cfg.bbbChampion||2,mp=cfg.matchPlayChampion||2;return{roundId,mode:"individual",max:bbb+mp,items:[{label:"Bingo-Bango-Bongo champion",pts:bbb},{label:"Match-play champion (best record)",pts:mp}],summary:`BBB champ ${bbb} · Match-play champ ${mp}`,note:"Individual round — the round-robin matches settle head-to-head. Ties for either title split the points."}}case"R6":{const win=cfg.singlesWin||3,halve=cfg.singlesHalve||1.5;const slots=cfg.stablefordFinish||[4,3,2,1];return{roundId,mode:"individual",max:win+(slots[0]||0),items:[{label:`Win your singles match (halve = ${halve} each)`,pts:win},...slots.map((pts,i)=>({label:`Finish ${nth(i)} in Stableford`,pts}))],summary:`Singles win ${win} (halve ${halve}) · Stableford finish ${slots.join(" / ")}`,note:"Championship night: singles seeded off the standings (1v2 for the title, 3v4 for bronze), plus an all-play individual Stableford that pays every finish spot. Ties split."}}default:return{roundId,mode:"individual",max:0,items:[],summary:"",note:""}}}function seasonAwardsBreakdown(model){const cfg=(model.pointsConfig||{}).seasonAwards||{};const items=[{label:"Skins King (most skins)",pts:cfg.skinsKing||2},{label:"Birdie King (most net birdies)",pts:cfg.birdieKingNet||2},{label:"Flat Stick (fewest putts)",pts:cfg.flatStickFewestPutts||1},{label:"Iron Man (most FIR + GIR)",pts:cfg.ironManFIRplusGIR||1}];return{items,max:items.reduce((a,x)=>a+x.pts,0),summary:items.map(x=>`${x.label.split(" (")[0]} ${x.pts}`).join(" · "),note:"Settled after R6 from R2–R6 play only. Ties split the award."}}function adjustedMaxPossible(model){const awardsTotal=Object.values(model.pointsConfig.seasonAwards||{}).reduce((a,v)=>a+(typeof v==="number"?v:0),0);const out={};model.players.forEach(p=>{let m=awardsTotal;model.rounds.forEach(r=>{if(STANDINGS_EXCLUDED_ROUNDS.includes(r.id))return;if(r.players.includes(p.id))m+=ROUND_MAX_POINTS[r.id]||0});out[p.id]=m});return out}return{STANDINGS_EXCLUDED_ROUNDS,ROUND_MAX_POINTS,rankedFinishPoints,pointsForRound,seasonAwards,compute,adjustedMaxPossible,roundPointsBreakdown,seasonAwardsBreakdown}})();if(typeof window!=="undefined"){Object.assign(window,{EgtPoints})}
