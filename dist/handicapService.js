const HandicapService=(function(){const MIN_INDEX=-10;const MAX_INDEX=54;function clampIndex(index){const n=parseFloat(index);if(isNaN(n))return 0;return Math.min(MAX_INDEX,Math.max(MIN_INDEX,n))}function courseHandicap(index,slope,rating,par,holeCount){const hi=clampIndex(index)*(holeCount===9?.5:1);const sl=slope>0?slope:113;const cr=typeof rating==="number"&&rating>0?rating:par;return hi*(sl/113)+(cr-par)}function playingHandicap(ch,allowancePct){const pct=typeof allowancePct==="number"&&allowancePct>=0?allowancePct:100;return ch*(pct/100)}function roundHandicap(x){return Math.round(x)}function _rankByStrokeIndex(holes){return holes.map((h,i)=>({i,hdcp:h.hdcp||i+1})).sort((a,b)=>a.hdcp-b.hdcp||a.i-b.i).map(x=>x.i)}function allocateStrokes(playing,holes){const n=holes.length;if(!n)return[];const h=Math.round(playing||0);const arr=holes.map(()=>0);if(h===0)return arr;const magnitude=Math.abs(h);const base=Math.floor(magnitude/n);const rem=magnitude%n;const sign=h>0?1:-1;const order=_rankByStrokeIndex(holes);if(sign<0)order.reverse();if(base)for(let i=0;i<n;i++)arr[i]=base*sign;for(let i=0;i<rem;i++)arr[order[i]]+=sign;return arr}function netScore(gross,strokesReceived){if(!gross)return 0;return Math.max(1,gross-(strokesReceived||0))}function playingHandicaps(players,holes,tee,opts){const o=opts||{};const par=holes.reduce((a,h)=>a+(h.par||0),0);const holeCount=holes.length;const raw=players.map(p=>{const idx=o.overrides&&o.overrides[p.id]!==void 0&&o.overrides[p.id]!==null&&o.overrides[p.id]!==""?clampIndex(o.overrides[p.id]):clampIndex(p.handicap||0);const ch=courseHandicap(idx,tee==null?void 0:tee.slope,tee==null?void 0:tee.rating,par,holeCount);const ph=playingHandicap(ch,o.allowancePct);return{p,idx,ch,ph}});const minPh=o.relative&&raw.length?Math.min(...raw.map(r=>r.ph)):0;const out={};raw.forEach(r=>{const effective=r.ph-minPh;const rounded=roundHandicap(effective);out[r.p.id]={index:r.idx,courseHcp:r.ch,playing:effective,rounded,strokes:allocateStrokes(rounded,holes)}});return out}function teamPlayingHandicap(memberCourseHcps,weightsPct){const sorted=memberCourseHcps.slice().sort((a,b)=>a-b);let total=0;sorted.forEach((ch,i)=>{const w=weightsPct[Math.min(i,weightsPct.length-1)]||0;total+=ch*(w/100)});return total}const _providers=[];function registerProvider(provider){if(!provider||!provider.id||typeof provider.fetchIndex!=="function"){throw new Error("HandicapService provider needs { id, fetchIndex }")}if(!_providers.some(p=>p.id===provider.id))_providers.push(provider)}function providerAvailable(){return _providers.length>0}function fetchIndex(player,cb){if(!_providers.length){cb&&cb(null,"No handicap service connected");return}_providers[0].fetchIndex(player,function(index,err){if(index===null||index===void 0||isNaN(parseFloat(index))){cb&&cb(null,err||"Handicap lookup failed");return}cb&&cb(clampIndex(index),null)})}return{MIN_INDEX,MAX_INDEX,clampIndex,courseHandicap,playingHandicap,roundHandicap,allocateStrokes,netScore,playingHandicaps,teamPlayingHandicap,registerProvider,providerAvailable,fetchIndex}})();if(typeof window!=="undefined"){Object.assign(window,{HandicapService})}
